import React from 'react';

// Simple mock for ReactMarkdown: renders its children (the text content)
const ReactMarkdownMock = ({ children }) => {
  return <>{children}</>;
};

export default ReactMarkdownMock;
